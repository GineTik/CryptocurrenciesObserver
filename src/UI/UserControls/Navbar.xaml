<UserControl x:Class="UI.UserControls.Navbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.UserControls"
             mc:Ignorable="d"
             d:DesignWidth="300" d:DataContext="{d:DesignInstance local:Navbar}">
    <Grid>
        <StackPanel x:Name="NavbarPanel" 
                    Orientation="Vertical">
            <local:StyledButton Icon="Home" Text="Home" IsActive="True" Click="NavbarButtonClick"></local:StyledButton>
            <local:StyledButton Icon="Exchange" Text="Convertor" Click="NavbarButtonClick"></local:StyledButton>
            <local:StyledButton Icon="Question" Text="Question" Click="NavbarButtonClick"></local:StyledButton>
        </StackPanel>
    </Grid>
</UserControl>
