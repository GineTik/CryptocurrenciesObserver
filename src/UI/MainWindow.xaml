<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1000">
    
    <Grid Background="{DynamicResource MainBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="150"></ColumnDefinition>
            <ColumnDefinition Width="500"></ColumnDefinition>
            <ColumnDefinition Width="250"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="1">
            <DockPanel>
                <TextBlock FontSize="18" FontWeight="600" Margin="4 20 0 40" TextWrapping="Wrap" DockPanel.Dock="Top">
                    Cryptocurrencies Observer
                </TextBlock>
                <StackPanel Orientation="Vertical" DockPanel.Dock="Top">
                    <Button Style="{StaticResource NavbarButtonActive}">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="Home" FontSize="30" />
                            <TextBlock VerticalAlignment="Center" Margin="10 0 0 0" FontSize="16">Home</TextBlock>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource NavbarButton}">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="Exchange" FontSize="30" />
                            <TextBlock VerticalAlignment="Center" Margin="10 0 0 0" FontSize="16">Convertor</TextBlock>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource NavbarButton}">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="Question" FontSize="30" />
                            <TextBlock VerticalAlignment="Center" Margin="10 0 0 0" FontSize="16">How to use</TextBlock>
                        </StackPanel>
                    </Button>
                </StackPanel>
                <Button Style="{StaticResource NavbarButton}" 
                        Foreground="{DynamicResource Red}" 
                        VerticalAlignment="Bottom"
                        Margin="0 0 0 20">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                        <fa:FontAwesome Icon="Times" FontSize="30" />
                        <TextBlock VerticalAlignment="Center" Margin="10 0 0 0" FontSize="16">Exit</TextBlock>
                    </StackPanel>
                </Button>
            </DockPanel>
        </Grid>
        <Grid Grid.Column="2" Background="{DynamicResource SecondaryBackground}" Margin="20 0 20 0"></Grid>
        <Grid Grid.Column="3" Background="{DynamicResource SecondaryBackground}"></Grid>
    </Grid>
</Window>
